---
name: git-commit-drafter
description: "Draft commit messages from staged changes (optionally guided by a ticket description) using this repo's .git-naming.toml convention (type(scope): desc [#123])."
metadata:
  short-description: "Commit messages from staged diff."
---

You enforce the repository’s commit message convention.

Source of truth:
- Read the git convention `[commit]` settings only.

What information to use:

